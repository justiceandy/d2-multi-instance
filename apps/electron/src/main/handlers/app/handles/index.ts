import onboard from './onboard.handle';
import discord from './discord.handle'
import github from './github.handle';
import metrics from './metrics.handle';
import paths from './paths.handle';
import patreon from './patreon.handle'
import version from './version.handle';


export {
    onboard,
    discord,
    github,
    metrics,
    paths,
    patreon,
    version,
}

export default {
    onboard,
    discord,
    github,
    metrics,
    paths,
    patreon,
    version,
}