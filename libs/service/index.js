import primary from "./primary";
import workers from "./workers";
import cluster from "./cluster";

export default {
    primary,
    workers,
    cluster,
}

export {
    primary,
    workers,
    cluster,
}