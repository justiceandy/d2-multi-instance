import { Store } from '@d2r/level-db'

/**
 * {@link clear} Remove all D2Data
 */
export const clear = async () => {
    // const { CharacterDB } = await Store();
    // await CharacterDB.clear();
    return true;
}
 