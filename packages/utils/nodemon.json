{
    "watch": ["src", "./dev.ts"],
    "ext": "ts,js,json",
    "ignore": ["node_modules", "coverage", "dist"],
    "exec": "ts-node --project ./tsconfig.json -r tsconfig-paths/register ./dev.ts",
    "restartable": "rs",
    "env": {
      "NODE_ENV": "development"
    }
  }
