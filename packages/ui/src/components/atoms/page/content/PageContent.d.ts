
import { ReactNode } from "react-router-dom/node_modules/@types/react";

export interface PageContentArgs {
    children?: ReactNode,
    form?: boolean,
    overlay?: boolean,
    container?: boolean,
}

