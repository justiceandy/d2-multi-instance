import ActiveButtons from "./ActiveButtons";
import AuthButtons from "./AuthButtons";
import IdleButtons from "./IdleButtons";
import LaunchingButtons from "./LaunchingButtons";
import QueButtons from "./QueButtons";
import RunningButtons from "./RunningButtons";
import TerminatingButtons from "./TerminatingButtons";

export default {
    ActiveButtons,
    AuthButtons,
    IdleButtons,
    LaunchingButtons,
    QueButtons,
    RunningButtons,
    TerminatingButtons,
}

export {
    ActiveButtons,
    AuthButtons,
    IdleButtons,
    LaunchingButtons,
    QueButtons,
    RunningButtons,
    TerminatingButtons,
}
